{
  "name": "@sentinel-vrt/action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify --source-map",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@sentinel-vrt/cli": "workspace:*"
  },
  "devDependencies": {
    "@sentinel-vrt/config": "workspace:*",
    "@vercel/ncc": "^0.38.3",
    "@types/node": "^25.3.3",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
